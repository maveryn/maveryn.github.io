{% assign profile = include.profile %}

<div class="repo repo-profile-card">
  <div class="card h-100 hoverable repo-native-card repo-native-card--profile">
    <div class="card-body">
      <div class="repo-native-card__eyebrow">
        <span class="repo-native-card__icon"><i class="fa-brands fa-github"></i></span>
        <span class="repo-native-card__path">{{ profile.username }}</span>
      </div>
      <h2 class="card-title repo-native-card__title">{{ profile.name }}</h2>
      <p class="card-text repo-native-card__description">{{ profile.description }}</p>
      <div class="link-cluster repo-native-card__links">
        {% for link in profile.links %}
          <a class="link-chip" href="{{ link.url }}">{{ link.label }}</a>
        {% endfor %}
      </div>
    </div>
  </div>
</div>
